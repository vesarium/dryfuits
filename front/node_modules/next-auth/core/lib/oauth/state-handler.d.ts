import { InternalOptions } from "src/lib/types";
/** Returns state if provider supports it */
export declare function createState(options: InternalOptions<"oauth">): string | undefined;
/**
 * Consistently recreate state from the csrfToken
 * if `provider.checks` supports `"state"`.
 */
export declare function getState({ provider, csrfToken }: InternalOptions<"oauth">): string | undefined;
